<?php
    //GET $_GET[''] 
    //in Get you can see the data and the browser save them
    //isset to do what you want when you press on the button and used for get also
    if(isset($_GET['sum']))
    {
        echo $_GET['n1']+$_GET['n2'];
    }
    ?>